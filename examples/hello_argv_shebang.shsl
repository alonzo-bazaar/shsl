#!./shsl
;; -*- mode:clojure -*-
;; vi: set ft=clojure;
(println (vec-len argv))
(if (== (vec-len argv) 0)
  (println "hello, nameless one")
  (if (== (vec-len argv) 1)
    (println "hello," (vec-get argv 0))
    (println "woah there buddy, that's way too many names!")))
