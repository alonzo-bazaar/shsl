#!./shsl
;; -*- mode:clojure -*-
;; :vi set ft=clojure:

;; (def ctadd (macro [a b] (+ a b)))
;; (def ctsub (macro [a b] (- a b)))
;; (def ctfst (macro [a b] a))
;; (def ctsnd (macro [a b] b))
;; (println ctadd)
;; (println (ctadd 10 20))
;; (println (macroexpand-1 '(ctadd 10 20)))
;; (println (macroexpand-1 '(ctadd (ctfst 5 10) (ctfst 5 10))))

(def defn (macro [name args & body]
                 (cons 'def
                       (cons name
                             (list (cons 'fn
                                         (cons args (vec->list body))))))))
